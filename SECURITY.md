# 安全规范

## 凭据管理

- 凭据仅通过服务端环境变量或部署平台 Secret 提供。
- `.env` 文件不得提交，示例文件不得包含真实值。
- JWT 密钥至少 32 个字符，并按环境独立配置。
- 凭据应遵循最小权限原则并定期轮换。

## 应用安全

- 所有输入、对象 ID、角色和资源所有权均在服务端校验。
- 浏览器来源使用 CORS 允许列表。
- 登录和高成本接口应设置请求频率限制。
- 上传文件必须校验大小、类型和文件签名。
- 日志不得记录密码、Token、授权头或用户上传内容。
- 依赖、测试、构建和凭据扫描由 CI 持续检查。

## 问题报告

请私下联系仓库维护者，并提供受影响版本、复现步骤、影响范围和脱敏日志。不要在公开 Issue 中提交凭据、Token、个人信息或完整日志。
